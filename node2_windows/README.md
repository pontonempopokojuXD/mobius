# Node 2 (Titan) — usunięty

Backend Titan został usunięty z projektu. MOBIUS korzysta wyłącznie z Ollama.

Aby przywrócić Titan (np. dla architektury distributed), użyj brancha `backup-with-titan`:

```bash
git checkout backup-with-titan
```
