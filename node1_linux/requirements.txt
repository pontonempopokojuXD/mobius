# ─────────────────────────────────────────────────────────
#  MOBIUS – Node 1 (Ubuntu Server 24.04 LTS / CPU-only)
#  Python 3.11+
#
#  System deps (run once):
#    sudo apt install portaudio19-dev libsndfile1 ffmpeg
# ─────────────────────────────────────────────────────────

# ── gRPC ─────────────────────────────────────────────────────────────────────
grpcio>=1.67.0
grpcio-tools>=1.67.0
protobuf>=5.28.0

# ── faster-whisper (CTranslate2, int8 CPU) ───────────────────────────────────
faster-whisper>=1.0.3

# ── WebRTC Voice Activity Detection ──────────────────────────────────────────
webrtcvad>=2.0.10

# ── PyAudio (microphone input) ───────────────────────────────────────────────
# Requires:  sudo apt install portaudio19-dev
PyAudio>=0.2.14

# ── Ollama Python library ─────────────────────────────────────────────────────
# Official client for the local Ollama daemon (localhost:11434)
# Install Ollama daemon: curl -fsSL https://ollama.com/install.sh | sh
# Pull model:            ollama pull llama3.2:1b
ollama>=0.3.0

# ── System metrics ───────────────────────────────────────────────────────────
psutil>=6.0.0

# ── NumPy ────────────────────────────────────────────────────────────────────
numpy>=1.26.0
