# ─────────────────────────────────────────────────────────
#  MOBIUS GUI — Lokalne Centrum Dowodzenia
#  CustomTkinter | Ollama | Tytan Monitor | Agent | Voice
# ─────────────────────────────────────────────────────────

customtkinter>=5.2.0
requests>=2.31.0
psutil>=6.0.0
pynvml>=11.5.0

# Głos — TTS (edge-tts, Microsoft, darmowy)
edge-tts>=6.1.0

# Głos — STT (SpeechRecognition)
# PyAudio wymaga portaudio; na Windows: pip install pipwin && pipwin install pyaudio
SpeechRecognition>=3.10.0

# RAG — baza wiedzy
chromadb>=0.4.0

# Titan (gRPC) — opcjonalnie dla backendu Titan
grpcio>=1.67.0
protobuf>=5.28.0

# Powiadomienia Windows (toast) — opcjonalnie
winotify>=1.1.0

# Lokalny STT + Wake Word (Whisper, offline)
faster-whisper>=1.0.3
sounddevice>=0.4.6

# Web search (agent tool)
duckduckgo-search>=6.0.0

# Vision / screenshots (agent tool)
Pillow>=10.0.0

# Naturalny parsing dat (przypomnienia)
dateparser>=1.1.8

# System integration (aktywne okno, schowek)
pygetwindow>=0.0.9
pyperclip>=1.8.2
